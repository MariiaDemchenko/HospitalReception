@using CacheMachine.Common
@model CacheMachine.ViewModels.NumDisplayViewModel
@{
    if (Model.Mask == Constants.PinCodeMask)
    {
        @Html.Partial("Numpad/PasswordNumDisplay", Model);
    }
    else
    {
        @Html.Partial("Numpad/TextNumDisplay", Model);
    }
}