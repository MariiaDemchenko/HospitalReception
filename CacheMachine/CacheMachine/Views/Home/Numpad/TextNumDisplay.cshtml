@{
    var className = string.IsNullOrEmpty(Model) ? string.Empty : "card-num";
}
@Html.TextBox("inputField", null, new { @class = $"num-display form-control  {className}", placeholder = Model, autocomplete = "off", autofocus = "true" })