@{
    var className = string.IsNullOrEmpty(Model) ? string.Empty : "card-num";
}
@Html.TextBox("inputField", null, new { @class = $"form-control {className}", placeholder = Model, autocomplete = "off", autofocus = "true" })