@using CacheMachine.Common
@using CacheMachine.ViewModels
@using (Html.BeginForm("CheckCardNum", "Home", FormMethod.Post, new { @class = "form-cache-machine" }))
{
    //TODO: separate Numpad and all the connected PartialViews correctly
    <div class="page-header">
        <h2>@Resources.InputCardNum</h2>
    </div>
    @Html.Partial("Numpad/Numpad", new NumDisplayViewModel { Id = "inputCardNum", Mask = Constants.CardNumMask })
}