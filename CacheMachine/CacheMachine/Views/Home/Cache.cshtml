@{
    ViewBag.Title = "Pin Code";
}
@using CacheMachine.Common

<content id="pinCodeContent">
    @using (Html.BeginForm("OperationResult", "Home", FormMethod.Post, new { @class = "form-cache-machine" }))
    {
        <div class="page-header">
            <h2>@Resources.GetCache</h2>
        </div>
        @Html.Partial("Numpad/Numpad", string.Empty)
        <div class="btn-menu">
            @Html.Partial("ExitButton")
        </div>
    }
</content>
<script>
    $(document).ready(function () {
        initNumpad("inputField", "sum");
    });
</script>